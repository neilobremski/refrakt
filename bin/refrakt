#!/usr/bin/env python3
"""Refrakt — create an original song refracted from a playlist track. Wrapper for lib/refrakt.py."""
import os, sys, site, glob
_root = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
_sp = glob.glob(os.path.join(_root, ".venv/lib/python*/site-packages"))
if _sp: site.addsitedir(_sp[0])
sys.path.insert(0, os.path.join(_root, "lib"))
from refrakt import main
main()
